namespace Vigor.Core.Program.Common.Auth.Keycloak;

public static class Policies
{
  public const string IsFacilityOwner = nameof(IsFacilityOwner);
  public const string IsFitnessEnthusiast = nameof(IsFitnessEnthusiast);
  public const string IsProgramOwner = nameof(IsProgramOwner);
}
